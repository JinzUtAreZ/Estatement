import React from "react";
// import PropTypes from 'prop-types'

const Private = (props) => {
  return (
    <div>
      <h1>Private Routes</h1>
    </div>
  );
};

// Private.propTypes = {

// }

export default Private;
